You are an execution adapter under the Token-Efficient Agent Harness.

P2-005 was reviewed and is BLOCKED_WITH_NOTES.

You are authorized to execute P2-005R only.

Execution Slice: P2-005R — Phase 2 Closeout Correction

Goal: Fix governance and evidence API contract gaps so Phase 2 can be sealed.

Required fixes:

1. Fix TASK_QUEUE.md — add P2-004 (done) and P2-005 (done) entries

2. Fix PROJECT_BOARD.md — mark P1-013 as done (Phase 1 is sealed), remove stale "Phase 1 closeout pending" text, add P2-005R row

3. Fix PHASE2_CLOSEOUT_REPORT.md — update sealed commit to include P2-005R commit, add correction note

4. Strengthen Evidence API service (services/evidence_reports.py):
   - build_evidence_status() must return status: "PASS"|"WARN"|"ERROR" with boundary_confirmations
   - load_optional_text_metadata() must return exists/line_count/contains_sealed_baseline/contains_final_gate_pass
   - summarize_active_yaml_validation() must include selected_branch, primary_candidate, calibration_ready
   - Status rules: PASS if all evidence exists and passes, WARN if phase1 closeout missing, ERROR if required JSON missing/invalid

5. Strengthen schemas (schemas/evidence_reports.py):
   - EvidenceStatusResponse: add boundary_confirmations field
   - Add EvidenceBoundaryConfirmations schema
   - Phase1CloseoutSummary: add line_count, contains_sealed_baseline, contains_final_gate_pass

6. Strengthen API router — /evidence/status must return boundary_confirmations

7. Strengthen tests (tests/test_evidence_reports_api.py):
   - Test status returns PASS/WARN/ERROR
   - Test boundary_confirmations fields
   - Test selected_branch == branch_D, primary_candidate == branch_D, calibration_ready == false
   - Test phase1_closeout contains_sealed_baseline, contains_final_gate_pass
   - Test endpoints don't expose full YAML/markdown
   - Test API doesn't modify active YAML
   - Test no score_*.yaml created
   - Test no archive folders created

8. Run full tests: PYTHONPATH=apps/api/src python -m pytest apps/api/tests/ -q
   Expect 110+ tests passing

9. Update PHASE2_CLOSEOUT_REPORT.md final gate if all checks pass

10. Git commit: 'P2-005R: Phase 2 Closeout Correction — governance and API contract gaps fixed'
11. Do NOT push

Do NOT implement new features. Do NOT expand scope.
