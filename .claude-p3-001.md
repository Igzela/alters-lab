You are an execution adapter under the Token-Efficient Agent Harness.

P3-000 has passed review. You are authorized to execute P3-001 only.

Execution Slice: P3-001 — Controlled Snapshot Write API

Goal: Add a governed API endpoint that can persist a confirmed in-memory snapshot session to alters/current/snapshot.yaml only after explicit human approval and governance validation.

Do NOT implement branches, alters, dialogue, value alignment, calibration, drift, archive, provider, database, or frontend.

Required implementation:

1. Create apps/api/src/alters_lab/services/snapshot_persist.py with:
   - sha256_text(text) -> str
   - sha256_file(path) -> str | None
   - build_snapshot_persist_payload(snapshot) -> dict
   - validate_snapshot_persist_governance(snapshot) -> dict with:
     - intake_status.phase == "completed"
     - intake_status.pending_anchor is None
     - all three anchors non-empty
     - evidence_policy.source == "human_provided"
     - no branch_discovery trigger field
   - persist_snapshot_to_disk(payload, target_path, approval_token) -> dict
     - Only writes if approval_token matches expected value
     - Produces audit record in docs/harness/phase3_write_audit.jsonl
     - Returns {status, path, sha256_before, sha256_after, audit_record}

2. Create apps/api/tests/test_snapshot_persist.py with unit tests for:
   - sha256_text produces correct hash
   - sha256_file returns None for missing file
   - build_snapshot_persist_payload produces valid dict
   - validate_snapshot_persist_governance catches missing anchors
   - validate_snapshot_persist_governance catches non-completed phase
   - persist_snapshot_to_disk rejects invalid approval_token
   - persist_snapshot_to_disk writes and produces audit record
   - Use tmp_path, do NOT mutate real alters/current/snapshot.yaml

3. Create apps/api/tests/test_snapshot_persist_api.py with API tests:
   - POST /snapshot-intake/persist endpoint exists
   - Returns 403 if approval_token missing
   - Returns 403 if approval_token wrong
   - Returns 200 with audit record if valid
   - Use TestClient, tmp_path, monkeypatch

4. Update apps/api/src/alters_lab/api/snapshot_intake.py to add:
   - POST /snapshot-intake/persist endpoint
   - Accepts approval_token in body
   - Calls persist_snapshot_to_disk
   - Returns audit record

5. Update apps/api/src/alters_lab/schemas/snapshot.py if needed for persist request/response

6. Run tests: PYTHONPATH=apps/api/src python -m pytest apps/api/tests/ -q
   Expect 118+ tests passing

7. Update docs/harness/PROJECT_BOARD.md: P3-001 running -> done
8. Update docs/harness/TASK_QUEUE.md: add P3-001 done
9. Record audit entry in docs/harness/phase3_write_audit.jsonl if any real write happened

10. Git commit: 'P3-001: Controlled Snapshot Write API'
11. Do NOT push
12. Do NOT change alters/current/snapshot.yaml in the final commit

Boundary confirmations:
- Only snapshot.yaml can be written
- Write requires explicit approval_token
- All writes produce audit record
- Tests use tmp_path, not real files
- No other YAML files modified
