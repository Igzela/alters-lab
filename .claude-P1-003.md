You are an execution adapter under the Token-Efficient Agent Harness.

P1-002H has been accepted. Repo hygiene is clean. You are now authorized to execute P1-003 only.

Project identity:
Alters Lab is a personal future-branch simulation, path decision calibration, and reality-trace review system.
It is NOT a content creator calibration system.

Do not treat yourself as the governance authority.
Do not expand scope.
Do not create PRs.
Do not merge.
Do not connect real LLM providers.
Do not add frontend code.
Do not add database persistence.
Do not implement Branch Discovery.
Do not implement Alter Generation.
Do not implement Dialogue.
Do not implement Value Alignment.
Do not implement Calibration.
Do not implement Archive.

Execution Slice: P1-003 — Snapshot YAML Persistence / Export Gate

Goal:
Add an explicit, controlled export path that can serialize a confirmed in-memory Snapshot session into the canonical Phase 0 YAML shape.

This slice must NOT automatically write alters/current/snapshot.yaml during normal confirmation.
It must provide export functions and tests only. If a write function exists, it must require an explicit target path and must not be called by the confirm endpoint.

Allowed files:
- apps/api/src/alters_lab/schemas/snapshot.py
- apps/api/src/alters_lab/services/snapshot_intake.py
- apps/api/src/alters_lab/services/snapshot_sessions.py
- apps/api/src/alters_lab/services/snapshot_export.py
- apps/api/tests/test_snapshot_export.py
- apps/api/tests/test_snapshot_api.py
- apps/api/tests/test_snapshot_schema.py
- apps/api/tests/test_snapshot_intake.py
- apps/api/pyproject.toml only if PyYAML is required
- apps/api/README.md
- docs/harness/PROJECT_BOARD.md
- docs/harness/TASK_QUEUE.md
- docs/harness/RUN_LOG.md
- docs/harness/EVIDENCE_INDEX.md
- docs/harness/DECISION_RECORD.md
- docs/harness/RISK_REGISTER.md

Forbidden files:
- apps/web/**
- packages/**
- .env
- .env.local
- any LLM provider config
- any database code
- any branch discovery implementation
- any alter generation implementation
- any dialogue implementation
- any value alignment implementation
- any calibration implementation
- any archive implementation
- alters/current/snapshot.yaml
- alters/current/branches.yaml
- alters/current/reality_trace.yaml
- alters/current/alters/**
- alters/current/dialogue/**
- alters/current/value_alignment/**
- alters/calibration/**
- alters/archive/**

Important:
Do NOT modify the real active alters/current/snapshot.yaml in this slice.
Use temp files in tests only.

Required implementation:

1. Create apps/api/src/alters_lab/services/snapshot_export.py

Implement:
- snapshot_to_canonical_dict(snapshot: Snapshot) -> dict
  Returns canonical YAML-ready dict with top-level "snapshot:".
  Requires snapshot.intake_status.phase == completed.
  Raises ValueError if snapshot is not completed.
  Must not mutate snapshot.

- snapshot_to_yaml(snapshot: Snapshot) -> str
  Returns YAML string.
  Requires completed snapshot.
  Raises ValueError if snapshot is not completed.
  Output must include top-level "snapshot:".
  Preserve Unicode. Preserve key order.

- write_snapshot_yaml(snapshot: Snapshot, target_path: Path, overwrite: bool = False) -> Path
  Requires completed snapshot.
  Writes YAML only to explicit target_path.
  If target_path exists and overwrite is False, raise FileExistsError.
  Use tests with tmp_path only.
  Do not hardcode alters/current/snapshot.yaml.
  Do not call this function from API confirm endpoint.

2. YAML dependency
Use PyYAML with yaml.safe_dump(sort_keys=False, allow_unicode=True).
Update apps/api/pyproject.toml minimally if adding PyYAML.

3. Tests
Create apps/api/tests/test_snapshot_export.py covering:
- completed snapshot exports to canonical dict
- exported dict has top-level "snapshot"
- anchors are preserved
- context is preserved
- intake_status.phase is completed
- snapshot_to_yaml returns string containing "snapshot:"
- snapshot_to_yaml includes all three anchor fields
- incomplete snapshot export is rejected
- write_snapshot_yaml writes to tmp_path
- write_snapshot_yaml rejects overwrite by default
- write_snapshot_yaml with overwrite=True works in tmp_path
- confirm endpoint still does NOT write alters/current/snapshot.yaml
- no branch or alter artifacts are created

Run all tests: cd apps/api && python -m pytest
Expected test count should increase from 33.

4. API behavior
Do not add an export API endpoint in P1-003.
P1-003 is service/export contract only.
The existing POST /confirm endpoint must remain in-memory only.

5. Documentation updates
Update apps/api/README.md, docs/harness/TASK_QUEUE.md, PROJECT_BOARD.md, DECISION_RECORD.md, RISK_REGISTER.md, RUN_LOG, EVIDENCE_INDEX.

Acceptance criteria:
- snapshot_export.py exists
- completed Snapshot can export to canonical dict and YAML string
- incomplete Snapshot export is rejected
- write_snapshot_yaml writes only to explicit target path in tests
- existing alters/current/snapshot.yaml is not modified
- confirm endpoint remains in-memory
- pytest passes
- no frontend/database/LLM/Branch/Alter/Dialogue/Value/Calibration/Archive added
