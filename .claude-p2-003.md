You are an execution adapter under the Token-Efficient Agent Harness.

P2-002 has been accepted:
- 84 tests passing
- Active YAML Chain Validator CLI exists
- CLI returns exit 0 + PASS
- --json outputs status=PASS, ok=true
- ACTIVE_YAML_VALIDATION_REPORT.json generated
- --output rejects alters/ paths
- active YAML unchanged
- no forbidden runtime added
- commit f66fb97 pushed to main

You are now authorized to execute P2-003 only.

Project identity:
Alters Lab is a personal future-branch simulation, path decision calibration, and reality-trace review system.
It is NOT a content creator calibration system.

Phase 2 objective:
Build a read-only runtime foundation over the sealed Phase 1 active YAML chain.

Do not treat yourself as governance authority.
Do not expand scope.
Do not create PRs.
Do not merge.
Do not connect LLM providers.
Do not add frontend code.
Do not add database persistence.
Do not implement Branch Discovery generation.
Do not implement Alter Generation generation.
Do not implement Dialogue runtime.
Do not implement Value Alignment runtime.
Do not implement Calibration scoring.
Do not implement Archive creation.
Do not mutate active YAML artifacts.
Do not create calibration scores.
Do not compute drift.
Do not modify rubric/state.
Do not create archive folders.

Execution Slice: P2-003 — Read-only Cycle Summary API

Goal:
Expose the sealed active YAML chain summary and validation result through read-only FastAPI endpoints.

This API must only read from the active YAML loader/validator created in P2-001 and P2-002. It must not generate, mutate, score, calibrate, archive, or call providers.

Allowed files:
- apps/api/src/alters_lab/main.py
- apps/api/src/alters_lab/api/__init__.py
- apps/api/src/alters_lab/api/cycle_summary.py
- apps/api/src/alters_lab/schemas/cycle_summary.py
- apps/api/tests/test_cycle_summary_api.py
- apps/api/README.md
- docs/harness/PROJECT_BOARD.md
- docs/harness/TASK_QUEUE.md
- docs/harness/RUN_LOG.md
- docs/harness/EVIDENCE_INDEX.md
- docs/harness/DECISION_RECORD.md
- docs/harness/RISK_REGISTER.md

Forbidden files:
- alters/current/**
- alters/calibration/**
- alters/archive/**
- apps/web/**
- packages/**
- .env
- .env.local
- any provider config
- any database code
- any frontend code
- any branch discovery generation runtime
- any alter generation runtime
- any dialogue runtime
- any value alignment runtime
- any calibration runtime
- any archive runtime

Important:
The API may read active YAML.
The API must not write active YAML.
The API must not expose full personal YAML content by default.
The API should return summaries, validation status, artifact metadata, and boundary confirmations only.

Required implementation:

1. Create read-only API router

Create:
- apps/api/src/alters_lab/api/cycle_summary.py
- apps/api/src/alters_lab/schemas/cycle_summary.py

Update main.py:
- keep existing /health
- keep existing snapshot intake router
- include cycle summary router
- do not add branch/alter/dialogue/calibration/archive routers

2. API endpoints

Base path: /cycle-summary

GET /cycle-summary/health
Response: {"status": "ok", "component": "cycle-summary", "mode": "read_only"}

GET /cycle-summary/current
Loads active YAML chain and returns a concise summary.
Response shape:
{
  "status": "ok",
  "read_only": true,
  "summary": {
    "snapshot_phase": "completed",
    "branch_count": 4,
    "alter_ids": ["alter_A", "alter_B", "alter_C", "alter_D"],
    "primary_candidate": "branch_D",
    "selected_branch": "branch_D",
    "reality_trace_status": "active",
    "day_14_gate": "completed",
    "day_30_gate": "...",
    "calibration_ready": false
  },
  "validation": {"ok": true, "errors": [], "warnings": []},
  "boundary_confirmations": {
    "read_only": true,
    "active_yaml_modified": false,
    "calibration_score_created": false,
    "drift_computed": false,
    "archive_created": false,
    "provider_used": false,
    "generation_runtime_used": false
  }
}

GET /cycle-summary/validation
Returns only validation result and artifact count.
Response: {"status": "PASS", "ok": true, "errors": [], "warnings": [], "artifacts_checked": 9}

GET /cycle-summary/artifacts
Returns artifact metadata only, not full YAML content.
Response: {"status": "ok", "read_only": true, "artifacts": [{"key": "snapshot", "path": "alters/current/snapshot.yaml", "exists": true}, ...], "count": 9}

3. Error behavior
If active YAML loading fails: Return HTTP 500 with {"status": "error", "error_type": "active_yaml_load_failed", "message": "..."}
If validation fails: /cycle-summary/current may still return 200 with validation.ok=false. /cycle-summary/validation returns 200 with status="FAIL", ok=false, and errors.

4. Schemas
In schemas/cycle_summary.py, define simple Pydantic response models: BoundaryConfirmations, CycleSummaryData, ValidationResponse, ArtifactInfo, ArtifactListResponse, CycleSummaryResponse, CycleSummaryHealthResponse. Keep models minimal.

5. Artifact metadata
Implement helper: relative_artifact_paths() -> list[dict] with exactly 9 canonical artifacts. Use active_yaml_paths() from loader.

6. Tests
Create apps/api/tests/test_cycle_summary_api.py with tests for:
- /cycle-summary/health returns status ok and mode read_only
- /cycle-summary/current returns 200, selected_branch=branch_D, primary_candidate=branch_D, branch_count=4, validation.ok=true, boundary_confirmations.read_only=true
- /cycle-summary/validation returns ok=true, artifacts_checked=9
- /cycle-summary/artifacts returns count=9, no full YAML content
- API does not modify active YAML (hash/text before and after)
- API does not create score_*.yaml or alters/archive/20* folders
- main.py still does not include generation routers

7. Documentation updates
Update apps/api/README.md with /cycle-summary endpoints documentation.
Update PROJECT_BOARD.md: P2-003 running/completed, P2-004 blocked/todo.
Update TASK_QUEUE.md, DECISION_RECORD.md, RISK_REGISTER.md, RUN_LOG, EVIDENCE_INDEX.

8. Required checks
Run full tests: cd apps/api && python -m pytest (report final count)
Run API import check to verify routes exist
Verify no active YAML changed
Verify no forbidden runtime routers

不push（我来push）。
