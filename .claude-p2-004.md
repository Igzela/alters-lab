You are an execution adapter under the Token-Efficient Agent Harness.

P2-003 has been accepted:
- 101 tests passing
- Read-only Cycle Summary API exists
- Endpoints: /cycle-summary/health, /current, /validation, /artifacts
- API returns read-only summaries and does not expose full YAML
- active YAML unchanged
- no forbidden runtime added
- commit 4a8fd15 pushed to main

You are now authorized to execute P2-004 only.

Project identity:
Alters Lab is a personal future-branch simulation, path decision calibration, and reality-trace review system.
It is NOT a content creator calibration system.

Phase 2 objective:
Build a read-only runtime foundation over the sealed Phase 1 active YAML chain and evidence artifacts.

Do not treat yourself as governance authority.
Do not expand scope.
Do not create PRs.
Do not merge.
Do not connect LLM providers.
Do not add frontend code.
Do not add database persistence.
Do not implement Branch Discovery generation.
Do not implement Alter Generation generation.
Do not implement Dialogue runtime.
Do not implement Value Alignment runtime.
Do not implement Calibration scoring.
Do not implement Archive creation.
Do not mutate active YAML artifacts.
Do not create calibration scores.
Do not compute drift.
Do not modify rubric/state.
Do not create archive folders.

Execution Slice: P2-004 — Read-only Evidence Report API

Goal:
Expose existing evidence reports through read-only FastAPI endpoints.

This API must read existing evidence artifacts only:
- docs/harness/DEMO_EVIDENCE_DAY30.json
- docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json
- docs/harness/PHASE1_CLOSEOUT_REPORT.md if present

It must not generate new evidence, mutate active YAML, compute calibration, or create runtime modules.

Allowed files:
- apps/api/src/alters_lab/api/evidence_reports.py
- apps/api/src/alters_lab/schemas/evidence_reports.py
- apps/api/src/alters_lab/services/evidence_reports.py
- apps/api/src/alters_lab/main.py
- apps/api/tests/test_evidence_reports_api.py
- apps/api/README.md
- docs/harness/PROJECT_BOARD.md
- docs/harness/TASK_QUEUE.md
- docs/harness/RUN_LOG.md
- docs/harness/EVIDENCE_INDEX.md
- docs/harness/DECISION_RECORD.md
- docs/harness/RISK_REGISTER.md

Forbidden files:
- alters/current/**
- alters/calibration/**
- alters/archive/**
- apps/web/**
- packages/**
- .env
- .env.local

Required implementation:

1. Create service layer: apps/api/src/alters_lab/services/evidence_reports.py
   - project_root() -> Path
   - evidence_paths() -> dict with day30_demo, active_yaml_validation, phase1_closeout paths
   - load_json_report(path) -> dict
   - load_optional_text_metadata(path) -> dict (for markdown closeout, metadata only, NOT full text)
   - summarize_day30_demo(report) -> dict
   - summarize_active_yaml_validation(report) -> dict
   - build_evidence_status() -> dict combining all summaries

2. Create schemas: apps/api/src/alters_lab/schemas/evidence_reports.py
   Minimal Pydantic models for responses.

3. Create API router: apps/api/src/alters_lab/api/evidence_reports.py
   Base path: /evidence
   Endpoints:
   - GET /evidence/health -> {"status": "ok", "component": "evidence-reports", "mode": "read_only"}
   - GET /evidence/status -> combined evidence status with day30_demo, active_yaml_validation, phase1_closeout summaries
   - GET /evidence/reports -> list of known evidence reports with exists flag
   - GET /evidence/day30-demo -> summarized Day 30 demo evidence
   - GET /evidence/active-yaml-validation -> summarized active YAML validation
   - GET /evidence/phase1-closeout -> closeout metadata only (NOT full markdown)

4. Update main.py to include evidence reports router.
   Allowed routers: snapshot intake, cycle summary, evidence reports.
   Forbidden: branch/alter/dialogue/calibration/archive runtime routers.

5. Error behavior:
   - Missing/invalid evidence JSON: return status=ERROR with explanation
   - Do not create missing files
   - Do not auto-fix evidence

6. Tests: apps/api/tests/test_evidence_reports_api.py
   Cover all endpoints, verify read-only, verify no YAML modification, verify no score/archive creation.

7. Documentation updates: README.md, PROJECT_BOARD.md (P2-004 done, P2-005 blocked/todo), TASK_QUEUE.md, DECISION_RECORD.md, RISK_REGISTER.md, RUN_LOG, EVIDENCE_INDEX.

8. Required checks:
   - Run full tests (report final count)
   - Verify API routes exist
   - Verify no active YAML changed
   - Verify no forbidden runtime routers

不push（我来push）。
