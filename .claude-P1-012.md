You are an execution adapter under the Token-Efficient Agent Harness.

Phase 1 Final Gate has passed with notes. You are now authorized to execute P1-012 only.

Project identity:
Alters Lab is a personal future-branch simulation, path decision calibration, and reality-trace review system.
It is NOT a content creator calibration system.

Do not treat yourself as the governance authority.
Do not expand scope.
Do not create PRs.
Do not merge.
Do not connect LLM providers.
Do not add frontend code.
Do not add database persistence.
Do not implement Branch Discovery runtime.
Do not implement Alter Generation runtime.
Do not implement Dialogue runtime.
Do not implement Value Alignment runtime.
Do not implement Calibration runtime.
Do not implement Archive runtime.
Do not mutate active YAML artifacts.
Do not create calibration scores.
Do not compute drift.
Do not modify rubric/state.
Do not create archive folders.

Execution Slice: P1-012 — Day 30 Demo Harness / Validator

Goal:
Create a bounded Day 30 demo harness that programmatically validates the CYCLE-001 v0.1 demo:
1. FastAPI app imports
2. /health works
3. Snapshot Intake session can be created
4. Three anchors can be submitted in order
5. Snapshot can be confirmed in memory
6. Confirm endpoint does not write active YAML
7. Confirmed snapshot can be exported to an explicit temporary YAML path
8. Active YAML chain exists and is structurally consistent
9. Tests pass
10. No forbidden components are used or claimed

Allowed files:
- apps/api/src/alters_lab/demo/__init__.py
- apps/api/src/alters_lab/demo/day30_harness.py
- apps/api/tests/test_day30_harness.py
- docs/harness/DEMO_EVIDENCE_DAY30.json
- docs/harness/PROJECT_BOARD.md
- docs/harness/TASK_QUEUE.md
- docs/harness/RUN_LOG.md
- docs/harness/EVIDENCE_INDEX.md
- docs/harness/DECISION_RECORD.md
- docs/harness/RISK_REGISTER.md

Forbidden files:
- alters/current/** (may READ but not WRITE)
- alters/calibration/**
- alters/archive/**
- apps/web/**
- packages/**
- .env
- any provider/database/frontend/runtime code

Required implementation:

1. Create apps/api/src/alters_lab/demo/__init__.py (empty)

2. Create apps/api/src/alters_lab/demo/day30_harness.py

Implement a read-only Day 30 demo harness with:
- run_day30_demo(output_path: Path | None = None, export_dir: Path | None = None) -> dict
  Uses FastAPI TestClient (no real server).
  Steps: health check, create session, submit 3 anchors in order, confirm, export to temp YAML, validate active YAML chain, verify no forbidden components.
  Returns machine-readable evidence dict.

- validate_active_yaml_chain() -> dict
  Checks: snapshot.yaml exists with phase=completed, branches.yaml exists with status=completed, 4 alter files exist with source_refs/quality_status, alignment file exists, dialogue file exists, reality_trace.yaml exists with status=active.
  Returns pass/fail per artifact.

- validate_no_forbidden_components() -> dict
  Checks: no .env, no provider config, no database code, no frontend code, no runtime dialogue/calibration/archive modules in apps/api/src/.

3. Create apps/api/tests/test_day30_harness.py

Tests covering:
- run_day30_demo returns evidence dict with all steps passing
- validate_active_yaml_chain passes
- validate_no_forbidden_components passes
- demo evidence report can be written to tmp_path
- harness does not modify active YAML (read snapshot before/after, assert equal)

4. Run all tests: cd apps/api && python -m pytest
Expected: existing 46 tests + new harness tests all pass.

5. Generate DEMO_EVIDENCE_DAY30.json by running the harness.

Acceptance criteria:
- day30_harness.py exists with run_day30_demo, validate_active_yaml_chain, validate_no_forbidden_components
- test_day30_harness.py exists with tests
- All tests pass
- DEMO_EVIDENCE_DAY30.json is generated
- No active YAML modified
- No new runtime modules implemented
- No frontend/database/provider added
