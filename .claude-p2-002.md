You are an execution adapter under the Token-Efficient Agent Harness.

P2-001 has passed conditional review:
- 62 tests passing
- Active YAML Loader package exists
- validate_active_yaml_chain returns ok=True
- summarize_active_yaml_chain returns selected_branch=branch_D and primary_candidate=branch_D
- active YAML unchanged
- no forbidden runtime added

You are now authorized to execute P2-002 only.

Project identity:
Alters Lab is a personal future-branch simulation, path decision calibration, and reality-trace review system.
It is NOT a content creator calibration system.

Phase 2 objective:
Build a read-only runtime foundation over the sealed Phase 1 active YAML chain.

Do not treat yourself as governance authority.
Do not expand scope.
Do not create PRs.
Do not merge.
Do not connect LLM providers.
Do not add frontend code.
Do not add database persistence.
Do not implement Branch Discovery generation.
Do not implement Alter Generation generation.
Do not implement Dialogue runtime.
Do not implement Value Alignment runtime.
Do not implement Calibration scoring.
Do not implement Archive creation.
Do not mutate active YAML artifacts.
Do not create calibration scores.
Do not compute drift.
Do not modify rubric/state.
Do not create archive folders.

Execution Slice: P2-002 — Active YAML Chain Validator CLI

Goal:
Create a read-only CLI command that validates the sealed active YAML chain using the P2-001 loader/validator and produces a machine-readable validation report.

This slice wraps the existing read-only loader logic. It must not implement generation/runtime modules.

Allowed files:
- apps/api/src/alters_lab/cli/__init__.py
- apps/api/src/alters_lab/cli/validate_active_yaml.py
- apps/api/tests/test_validate_active_yaml_cli.py
- docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json
- docs/harness/PROJECT_BOARD.md
- docs/harness/TASK_QUEUE.md
- docs/harness/RUN_LOG.md
- docs/harness/EVIDENCE_INDEX.md
- docs/harness/DECISION_RECORD.md
- docs/harness/RISK_REGISTER.md

Allowed only if necessary:
- apps/api/pyproject.toml

Forbidden files:
- alters/current/**
- alters/calibration/**
- alters/archive/**
- apps/web/**
- packages/**
- .env
- .env.local
- any provider config
- any database code
- any frontend code
- any branch discovery runtime
- any alter generation runtime
- any dialogue runtime
- any value alignment runtime
- any calibration runtime
- any archive runtime

Important:
The CLI may read active YAML files.
The CLI may write only docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json when explicitly requested.
The CLI must not modify alters/current, alters/calibration, or alters/archive.

Required implementation:

1. Create CLI package

Create:
- apps/api/src/alters_lab/cli/__init__.py
- apps/api/src/alters_lab/cli/validate_active_yaml.py

2. Implement CLI module

The CLI must be runnable as:
python -m alters_lab.cli.validate_active_yaml

Required behavior:
- Load active YAML chain using load_active_yaml_chain()
- Validate using validate_active_yaml_chain()
- Summarize using summarize_active_yaml_chain()
- Print concise human-readable result to stdout.
- Exit code:
  - 0 if validation ok
  - 1 if validation fails
  - 2 if loading fails due to missing/unparseable YAML or unexpected exception

CLI options:
--json: Print JSON report to stdout instead of human-readable text.
--output PATH: Write JSON report to explicit output path. Parent directory may be created if needed. Must reject output path under alters/current, alters/calibration, alters/archive. If output path is forbidden, exit 2.
--project-root PATH: Optional explicit repo root. Default uses default_project_root() from loader.
--strict: Treat warnings as failure. Current sealed baseline should pass.

3. JSON report shape

{
  "report_id": "ACTIVE-YAML-VALIDATION",
  "schema_version": 1,
  "status": "PASS",
  "ok": true,
  "errors": [],
  "warnings": [],
  "summary": {
    "snapshot_phase": "completed",
    "branch_count": 4,
    "alter_ids": ["alter_A", "alter_B", "alter_C", "alter_D"],
    "primary_candidate": "branch_D",
    "selected_branch": "branch_D",
    "reality_trace_status": "active",
    "day_14_gate": "completed",
    "day_30_gate": "...",
    "calibration_ready": false
  },
  "artifacts_checked": [
    "alters/current/snapshot.yaml",
    "alters/current/branches.yaml",
    "alters/current/alters/alter_A.yaml",
    "alters/current/alters/alter_B.yaml",
    "alters/current/alters/alter_C.yaml",
    "alters/current/alters/alter_D.yaml",
    "alters/current/value_alignment/alignment_2026-05-19.yaml",
    "alters/current/dialogue/dialogue_alter_D_2026-05-19.yaml",
    "alters/current/reality_trace.yaml"
  ],
  "boundary_confirmations": {
    "read_only_validation": true,
    "active_yaml_modified": false,
    "calibration_score_created": false,
    "drift_computed": false,
    "archive_created": false,
    "llm_provider_used": false,
    "frontend_added": false,
    "database_added": false,
    "generation_runtime_added": false
  }
}

If validation fails: status = "FAIL", ok = false, include errors, exit 1.
If load/runtime error occurs: status = "ERROR", ok = false, include error message, exit 2.

4. Safety guard

Implement:
is_forbidden_output_path(path: Path, project_root: Path) -> bool

Return true if resolved output path is inside:
- project_root / "alters" / "current"
- project_root / "alters" / "calibration"
- project_root / "alters" / "archive"

The CLI must reject forbidden output paths.

5. Tests

Create apps/api/tests/test_validate_active_yaml_cli.py

Tests should cover:
- CLI module imports.
- main([]) returns 0 on sealed baseline.
- --json prints valid JSON with ok=true and status=PASS.
- --output writes report to tmp_path only when explicitly requested.
- --output under alters/current is rejected.
- --project-root works when pointed at repo root.
- report includes 9 artifacts_checked.
- report summary selected_branch == branch_D.
- report summary primary_candidate == branch_D.
- report boundary_confirmations.read_only_validation is true.
- CLI does not modify: alters/current/snapshot.yaml, alters/current/branches.yaml, alters/current/reality_trace.yaml
- CLI does not create score_*.yaml.
- CLI does not create alters/archive/20* folders.

Testing approach:
- Prefer implementing main(argv: list[str] | None = None) -> int.
- Use pytest capsys for stdout.
- Use tmp_path for output tests.
- Do not write docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json during unit tests unless explicitly testing output to tmp_path.

6. Generate committed validation report

After implementation and tests, run the CLI once to generate:
docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json

The report must show: status = PASS, ok = true, selected_branch = branch_D, primary_candidate = branch_D, branch_count = 4, calibration_ready = false, 9 artifacts checked.

7. Documentation updates

Update PROJECT_BOARD.md: Phase 2 section with P2-001 done, P2-002 running/completed, P2-003 blocked/todo.
Update TASK_QUEUE.md: Add/update P2-002, add P2-003 blocked/todo.
Update DECISION_RECORD.md: "Active YAML validation is exposed first as a read-only CLI before any new runtime module."
Update RISK_REGISTER.md: Add risks for CLI accidental writes, report mistaken for calibration, validator drift, output path overwrite, future runtime before validator stable.
Update RUN_LOG and EVIDENCE_INDEX: Record P2-002.

8. Required checks

Run full tests: cd apps/api && python -m pytest (report final count)
Run CLI human: python -m alters_lab.cli.validate_active_yaml (expect exit 0, PASS)
Run CLI JSON: python -m alters_lab.cli.validate_active_yaml --json (expect valid JSON, status PASS)
Generate report: python -m alters_lab.cli.validate_active_yaml --output ../../docs/harness/ACTIVE_YAML_VALIDATION_REPORT.json
Verify no active YAML changed: git diff -- alters/current alters/calibration alters/archive
Verify no forbidden runtime: grep -R "branch.*router\|alter.*router\|dialogue.*router\|calibration.*router\|archive.*router" apps/api/src/alters_lab/main.py apps/api/src/alters_lab/api || true

不push（我来push）。
